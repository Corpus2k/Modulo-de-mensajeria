import { CanalForm } from '../components/CanalForm';

export const WhatsApp = () => {
	return (
		<CanalForm
			title={'WhatsApp'}
			campo={'mensaje'}
			canal={'whatsapp'}
			label={'Asunto'}
		/>
	);
};
